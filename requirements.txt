# FraudStream — Dependencies
# Phase 1: EDA
pandas>=2.0
numpy>=1.24
matplotlib>=3.7
seaborn>=0.12
scipy>=1.10
pytest>=7.0

# Phase 2: Batch pipeline (cleaning, training, evaluation)
scikit-learn>=1.3
xgboost>=2.0
tensorflow>=2.15
mlflow>=2.10

# Phase 3: Streaming ingestion (Kafka & Parquet)
confluent-kafka>=2.0
pyarrow>=12.0

# Phase 5: Real-time scoring service
fastapi>=0.110
uvicorn>=0.27

# Phase 6: Data versioning & model governance
dvc>=3.0
